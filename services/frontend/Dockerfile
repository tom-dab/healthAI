# Frontend Dockerfile — À compléter par Hélie
# Décommenter et adapter selon le choix technologique

# ─── Option A : Node.js (React / Vue) ────────────
# FROM node:20-alpine
# WORKDIR /app
# COPY package*.json ./
# RUN npm install
# COPY . .
# EXPOSE 3000
# CMD ["npm", "run", "dev"]

# ─── Option B : Serveur statique (HTML/CSS/JS) ───
# FROM nginx:alpine
# COPY . /usr/share/nginx/html
# EXPOSE 3000
